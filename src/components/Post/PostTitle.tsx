const PostTitle = ({ children }) => (
  <h2 className="p-2 text-center text-3xl font-bold leading-tight tracking-tighter sm:text-5xl md:text-6xl md:leading-none">
    {children}
  </h2>
);

export default PostTitle;
